<h1>Editing client_event</h1>

<% form_for(@client_event) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @client_event %> |
<%= link_to 'Back', client_events_path %>