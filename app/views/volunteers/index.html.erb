<h1>Listing volunteers</h1>

<table>
  <tr>
  </tr>

<% @volunteers.each do |volunteer| %>
  <tr>
	<td><%= link_to "#{volunteer.first_name} #{volunteer.last_name}", volunteer %></td></tr><tr>
    <!--<td><%= link_to 'Show', volunteer %>&nbsp<%= link_to 'Edit', edit_volunteer_path(volunteer) %>&nbsp<%= link_to 'Destroy', volunteer, :confirm => 'Are you sure?', :method => :delete %></td>-->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New volunteer', new_volunteer_path %>